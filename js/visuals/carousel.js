const swiper = new Swiper('.swiper-container', {
    loop: true,            // continuous loop
    autoplay: { delay: 2300 }, // auto-slide every 4 seconds
    slidesPerView: 1,      // show 1 review at a time
    speed: 600,             // smooth transition
});